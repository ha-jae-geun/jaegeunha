# 논리회로
```java
단일 종류의 논리 게이트(gate)만을 사용하더라도 모든 조합논리회로를 구현할 수 있는 게이트로 옳은 것은?
①	AND 게이트	 	②	OR 게이트
③	NOR 게이트		④	인버터(inverter)

유니버셜(Universal) 게이트
∙ 단일 게이트로 모든 조합논리 회로를 구현할 수 있는 범용게이트
∙ NAND 게이트와 NOR 게이트


17. 유니버설 게이트 집합(universal gate set)은 그 구성
원소만으로 어떤 형태의 디지털 시스템도 구현할 수 있는
기능적으로 완전한 게이트들의 집합이다. 다음 중 유니버
설 게이트 집합으로 옳지 않은 것은?
1 { NAND }
2 { OR, DECODER }
3 { MULTIPLEXER }
4 { OR, NOT }
5 { XOR }

답 5
5 XOR 게이트는 유니버셜 게이트가 될 수 없다.
<오답 체크> 3 일반적으로 이론서에는 NAND 게이트나 NOR 게
이트만 유니버셜 게이트라고 나와있다. 하지만 멀티플렉서
(MULTIPLEXER) 역시 입력선 A, B에 0이나 1의 입력선을 추가
해 어떻게 순서를 배치하느냐에 따라 모든 게이트를 표현할 수
있다.
예를 들어 입력선 x, y, z에 대해 멀티플렉서의 식을 (xz` + yz)로
설정할 경우, x에 0을 입력, y에 B를 입력, z에 A를 입력하면 출
력값 F = 0·A` + B·A = A·B가 되어 AND 게이트가 된다.
반면 x에 B를 입력, y에 1을 입력, z에 A를 입력하면 출력값
F = B·A` + 1·A = B·A` + A = (B+A)·(A`+A) = A + B가 되어
OR 게이트가 된다.
24 OR 게이트 하나로는 유니버셜 게이트가 될 수 없지만, OR 게
이트와 NOT 게이트를 활용하면 NOR 게이트를 만들 수 있기
때문에 OR와 NOT의 집합은 유니버셜 게이트 집합이 된다. 디
코더(DECODER)로 NOT 역할만 하도록 만들면 OR와 합쳐
NOR 게이트와 같도록 만들 수 있다.
```
