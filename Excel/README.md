# 단축키
* 셀 서식: Ctrl + 1
* 셀 삭제: Ctrl + + / -(직접 하면 창 안뜨고 셀에서 하면 창 뜸)
* 셀 삽입: 컨트롤 +/-(여러 개) | 홈 – 셀 – 삽입(일부) | 셀 클릭 – 시프트 드래그(한 개)
* 셀 이름 만들기: Ctrl + F3 -> 이름 삽입: F3 -> 이름: Ctrl + Shift + F3 | 수식 – 정의된 이름
* 숨기기: Ctrl+9 / Ctrl+0

## 무
```java
현재의 셀을 편집: F2 마지막 작업 반복: F4 이동하기: F5
다음 틀로 이동하기: F6 맞춤법: F7 영역 선택하기: F8 모두 다시 계산: F9
풀 다운 메뉴: F10 차트 만들기: F11 다른 이름으로 저장: F12
첫 행으로: HOME 다음 셀로 이동하기: TAB 다음 행 이동, 입력, 링크 실행하기: ENTER


<Shift>
셀 삽입, 삭제: Shift+ +- 메모: Shift + F2 함수: Shift + F3 팝업: Shift + F10


<Ctrl>
새 시트 추가: Shift + F11
Ctrl + F1: 작업창, 미리보기: Ctrl + F2, 이름 변경: Ctrl + F3, 계단식
창보기: Ctrl + F5/창 전환: Ctrl + F6/창 내리기: Ctrl + F9 (^Ctrl + N으로 창)
메크로 추가: Ctrl + F11


<F3>
F3: 이름 사용, Shift F3: 함수 이름, Ctrl + F3: 이름 만들기


<F11>
그냥 F11: 차트(<>Alt F1), Ctrl: 메크로(!메크로), Shift: 시트(!시트)
```

# 기호
## $
* 참조 유형

```java
ᆞ상대 참조: A1, B1
ᆞ절대 참조: $A$1, $B$1
ᆞ혼합 참조: $A1, A$1

=countifs($B$3:B46,">=300",$B$3:B46,"<=400")

```


## &(함수 연결 연산자)
* 연결 연산자: #&; 문자열을 여러 개 연결하여 하나로 만들거나 특정 값끼리 구분하는 기호
* =countif(F80:F1000,0.4) & "회"

```java
(결과는 항상 텍스트로 나타난다.) *=함수&“ ”
  *=RANK(O5,$E$5:$E$12)&"위“
```



## 기타 기호
```java
ᆞ0: 숫자 자리수 맞춤
ᆞ@: 특정 문자 표현. @님
ᆞ#: 유효한 숫자 표현 시

#,##0,, ; 만 단위 아래 0 사라짐, #,##0, ; 천단위(,가 하나 없음)

ᆞ(: ※중괄호 안되고 ( ) 여러번 사용
Sum(Sum(A!, A2), A3)같이 함수 안에 함수 가능
ᆞ?: 0을 공백으로 표시하여 자릿수 맞춤
ᆞ.: 소수점
ᆞ,: 천 단위로 숫자 구분
ᆞ\, $: 돈 단위
```




# 중복
1. 데이터 -중복 데이터 제거(맨 위, 빈칸 채움) 
2. 홈 - 조건부 서식 - 셀 강조 규칙 - 중복 값





# 함수
## Countif
* =countif(F80:F1000,0.4) & "회"

### 다중조건
* =countifs(B3:B38,">100",B3:B38,"<150")




## 반올림
* 버림: Rounddown


## counta
* 참석 횟수: counta해서 아무것도 안 쓰여 있는 함수 사용



## concat
* 로드맵, 교육과정 ; concat함수해서 두 개 비교해가며 사용


## 숫자 관련 함수
```java
분수: 0이상의 숫자 입력 후 한 칸 띄고 분자/분모, 12자리 이상 입력하면 지수 처리
#AVERAGEA(범위); 텍스트는 0으로 처리되어 평균 구해줌, TRUE는 1로 인식
#AVERAGEIF(범위, 조건, 평균을 구할 범위) ^SUMIF와 유사
#COUNTIF(범위, 값) ; 값에 해당하는 셀의 개수 구해줌 *=COUNTIF(C3:C8,">="&C6); !>문자
열, &필수
*=IF(COUNTIF(범위,숫자),1,2)
*= COUNTIF(D5:D12,"운동화")

#DAVERAGE(범위, 계산 필드, 조건)

*=ROUND(DAVERAGE(B4:J12,G4,D4:D5),0)
#DSUM( 데이터베이스, 필드, 조건 ): '데이터베이스' 범위에서 '조건' 범위의 설정을 만족하는
레코드를 추출하여, 그 중에서 합계를 구할 '필드' 의 합을 구하는 것이다.
※- 조건으로 레코드 추출하는 방법은 고급필터와 같다.
- 두번째 인수인 필드( 열 )의 합을 구하여 결과로 반환한다.
*현대자동차 렌드비용의 합계: =DSUM(B4:H12,7,E4:E5); 표 전체에서 7번째 렌트 비용
의 합
#MAX(범위); 최대값 구하기, #MAXA(범위); 텍스트 포함 최대값 구하기(별로)
#MOD(숫자, 숫자) ; 나머지 값
#MINA(범위); 텍스트를 0으로 인식해 최소인 값을 구하는 식
#PRODUT(값, 값); 곱셈
#STDEV.S/P ; 4가지 종류 ; 표준편차 ; P: 모집단, A: 텍스트와 논리값 포함
#SUBTOTAL(함수 번호, 범위 1, 범위 2) ; 함수 번호 1: 평, 2: count~~
#SUMIF(조건에 맞는 셀을 찾을 범위, 조건, 합을 구할 범위) ^AVERAGEIF와 유사
※1:1 대응되어야 하므로, 조건에 맞는 셀을 찾을 범위의 모양과 크기가 갖아야 한다.
#SUMPRODUCT(배열, 배열, 배열...); 배열 또는 범위의 대응되는 값끼리 곱해서 그 합을 구한다.
#VAR.S/P: 분산
#QUOTIENT(A,B); A를 B로 나눈 몫(정수)
```





## Is 함수
```java
ᆞ#ISBLANK(주어진 셀이 비어 있는 경우), #ISERR(#N/A를 제외한 오류인 경우), #ISNA: 셀 값
이 #N/A 오류인 경우, #ISERROR: 셀 값이 오류인 경우, #ISEVEN: 짝수인 경우, #ISODD: 홀수
인 경우
#ISTEXT: 텍스트인 경우, ISNONTEXT: 텍스트가 아닌 경우, ISNUMBER: 값이 수인 경우,
#ISREF: 값이 셀 주소인 경우
```




## 기타 함수
```java
#AND( , ) ; = AND((A1=A2), (A3=A4)) ※숫자 0 false로 인식, 나머지는 True
#COUNT(범위) ; 숫자가 쓰여있는 셀의 개수 구해줌
#COUNTA(범위); 비어 있지 않은 셀의 개수
*렌트 기간 전체 중 3일 이상 구하기 =COUNTIF(F5:F12,">=3")/COUNTA(F5:F12)
#COUNTBLANK(범위); 비어 있는 셀의 개수
#COUNTIF(범위, 값) ; 값에 해당하는 셀의 개수 구해줌 *COUNTIF(C3:C8,“>=”&80); !>문자열,
&필수
*렌트 기간 전체 중 3일 이상 구하기 =COUNTIF(F5:F12,">=3")/COUNTA(F5:F12)
#=DSUM( 데이터베이스, 필드, 조건 ): '데이터베이스' 범위에서 '조건' 범위의 설정을 만족하는
레코드를 추출하여, 그 중에서 합계를 구할 '필드' 의 합을 구하는 것이다.
※- 조건으로 레코드 추출하는 방법은 고급필터와 같다.
- 두번째 인수인 필드( 열 )의 합을 구하여 결과로 반환한다.
#IF(조건, 참 일 때 출력, 거짓일 때 출력) !중첩이 될 때는 =을 빼고 사용 =은 수식을 나타내는 기
호
*IF((총점)>=180, IF((총점)>=190, “장학금”, “합격”), “불합격”) !if((로 평범하게

*IF((상위%)<=4,1,IF((상위%)<=11,IF((상위%)<=23))) !IF((로 평범하게 + 뒤에 참값 + )
맨 뒤

*=IF(RIGHT(B6,1)="M","남성용",IF(RIGHT(B6,1)="W","여성용",""))

*~이 아니면: =IF(NOT(A1>A2),1,2)
*2초마다 바뀌기: **눈 내리기: =IF(MOD(SECOND(NOW()),2)=1, "*", “”)

**눈 반짝이기: 서식 – 색 – 투명한 색 설정 – 부분 / 배경 제거 – 제거 -> 조건부
MOD(SECOND(NOW())

*if 중첩+모두 참 값: =IF(LEFT(B5,2)="MC","중형",IF(LEFT(B5,2)="LC","대
형",IF(LEFT(B5,2)="RV","승합")))
#IFERROR(값, 오류일 때 표시되는 값) ※정상일 땐 그냥 값이 나옴
=IF(IFERROR(B4/C4“E”)=“E”,0,B4/C4) ; B4 나누기 C4의 값이 E이면 0, 아니면 다시
나누기
#INT *INT(RAND()*100): 1~100 사이의 정수
#LARGE(범위, 숫자): N번째로 높은 값 ^#SMALL
#LOOKUP(찾을 값, 값을 찾을 범위, 대응할 범위) !N번째에서 첫 번째 값 찾기 가능(<>V, H)
<>MATCH
#NOW *현재 시간을 초값으로: =SECOND(NOW())
#OR ※숫자 0 false로 인식, 나머지는 True
#RAND() ; 0부터 1사이의 아무 수나 출력함.
*=RAND()*N | =RAND()*(B-A)+A
*INT(RAND()*100): 1~100 사이의 정수 *INT(RAND()*50): 0~49 사이의 정수 ※ +1 하면 딱 됨
#RANK *=RANK(O5,$E$5:$E$12)&"위"
#RANK.EQ(등수를 구하려는 셀, 범위, 차순) ; ※ 범위 $로 고정해주어야 함(-> RANK.AVG) ; 0:
내림
*=RANK( ) / 총 개수: 상위 % 구하기 -> 셀 서식-백분율 – 소수 자릿수 1
#RANK.AVG(등수를 구하려는 셀, 범위); 공동은 ?.5로 나옴
#SUMIF(조건에 맞는 셀을 찾을 범위, 조건, 합을 구할 범위) ^AVERAGEIF와 유사
※1:1 대응되어야 하므로, 조건에 맞는 셀을 찾을 범위의 모양과 크기가 갖아야 한다.
#today(=Ctrl + ;)
#VLOOKUP(찾을 값, 표 범위, N, 논리값) ; V: Vertical ABC !~를 ~범위에서 ~칸 대충
표 범위는 $로 고정, N은 몇 번째 칸값 반환, 논리값 T는 대략|F는 정확히
*상위 %는 100-상위 해서 백분위를 만들어 높은 값이 높게 나오게 해준다.
== Index, match
함수 !함수명은 영어로 이해하고, 인수명은 간단히 나만의 한글로 번역해 놓는다, 쉼표 기억
) 생략하고 엔터쳐도 된다.
#=( , ) ; 괄호식이 참이면 True, 거짓이면 False ; 대소문자 구분하지 않는다.
#AND( , ) ; = AND((A1=A2), (A3=A4))
#AVERAGE; 백분율, 소수점 고려
#AVERAGEA(범위); 텍스트는 0으로 처리되어 평균 구해줌, TRUE는 1로 인식
#AVERAGEIF(범위, 조건, 평균을 구할 범위) ^SUMIF와 유사
#CEILING(수, N) 몇의 배수로 올림
#COUNT(범위) ; 숫자가 쓰여있는 셀의 개수 구해줌
#COUNTA(범위); 비어 있지 않은 셀의 개수
#COUNTBLANK(범위); 비어 있는 셀의 개수
#COUNTIF(범위, 값) ; 값에 해당하는 셀의 개수 구해줌 *COUNTIF(C3:C8,“>=”&80); !>문자열,
&필수
#COLUMN(값); 해당 셀의 열 번호를 구한다.

#DATE(년, 월, 일) ※사용자 서식 가서 형식 정하기 #YEAR(날짜) #MONTH(날짜) #DAY(날짜)
#DATEIF(“시작날짜”, “끝 날짜”, “00”) ; 사이 간격 ; Y(연수), M(달), D(일),
YD(앞에 연도 기준 뒤, 뒤 날짜 – 앞 날짜), YM(연도 고려 않고 월뺀 ‘만’ 값), MD(연도 x
일)
#EDATE(날짜,N); 해당 날짜에서 N일 후의 날짜
#EMONTH(날짜,N); 해당 날짜에서 N개월 후의 날짜 *+1하면 다음 달 1일 나옴
#EOMONTH(날짜,N); 해당 날짜에서 N개월 후의 마지막 일
#LARGE(범위, 숫자): N번째로 높은 값 ^#SMALL
#LEFT *“네이버”&LEFT(“블로그씨”, 3)
#LEN(셀) ; 셀의 길이를 잰다
#LENB(셀) ; 셀의 길이를 바이트 단위로 잰다.(영어, 숫자 1바이트, 한글: 2바이트)
#LOOKUP(찾을 값, 값을 찾을 범위, 대응할 범위) !N번째에서 첫 번째 값 찾기 가능(<>V, H)
<>MATCH
#FLOOR((수, N) ; 몇의 배수로 내림
#GETPIVOTDATA("금액",Sheet1!$A$3,"관할지역", A4) ; 1. Sheet1 시트의 ‘값’영역에 있는 데
이터 필드 이 름, 2. 특정 시트의 어느 셀이 위치한 곳의 피벗 테이블에서 가져온 데이터라는 것을
명시 3. 가져온 데이터를 지칭하는 필드 이름, 4 필드의 항목 이름
#HLOOKUP(찾을 값, 표 범위, N, 논리값) 123
#HYPERLINK(“사이트 주소”, “나타낼 주소”) ※ “ ”가 중요
#IF(조건, 참 일 때 출력, 거짓일 때 출력)
*~이 아니면: =IF(NOT(A1>A2),1,2)
*IF((총점)>=180, IF((총점)>=190, “장학금”, “합격”), “불합격”) !if((로 평범하게
*IF((상위%)<=4,1,IF((상위%)<=11,IF((상위%)<=23))) !IF((로 평범하게 + 뒤에 참값 + )
맨 뒤
#IFERROR(값, 오류일 때 표시되는 값) ※정상일 땐 그냥 값이 나옴
=IF(IFERROR(B4/C4“E”)=“E”,0,B4/C4) ; B4 나누기 C4의 값이 E이면 0, 아니면 다시
나누기
#INDIRECT(텍스트 셀 주소, 표시 형식) ; 표시 형식 TRUE/FALSE ; TRUE하면 A1, FALSE하면
R_C_
*=INDIRECT(“R”&H4+2&“C”&H5+2,FALSE) ※텍스트와 구분하기 위해 &을 쓰고, +2
가 가능
#INDEX(범위, 행 번호, 열 번호) ※INDEX((범위,범위),행 번호,열 번호,?) ; ?는 몇 번째 범위 쓸
지
#INT(숫자) ; 어떤 수보다 크지 않은 정수 중 가장 큰 정수
#ISERROR(값) ; 참, 거짓 결과 출력함 *IF(ISERROR())
#LEN(“텍스트”) ; 텍스트의 문자 수 구하는 함수
*Right(B3, D3-1)
#MAX #MIN(범위)
#MEDIAN(범위) ; 중간 값
#MID(텍스트, A, B) ; 텍스트 내의 A번째 문자부터 B개의 문자를 출력하는 것이다.
#MINA(범위); 텍스트를 0으로 인식해 최소인 값을 구하는 식
#MOD(숫자, 숫자) ; 나머지 값
#MODE(범위) ; 최빈값
#MROUND(수, N) ; N의 배수가 되도록 반올림한다.
*상위 %는 100-상위 해서 백분위를 만들어 높은 값이 높게 나오게 해준다.
#NOT(값) ; ~이 아니면 표현 가능
#PRODUT(값, 값); 곱셈
#RAND() ; 0부터 1사이의 아무 수나 출력함.

*=RAND()*N | =RAND()*(B-A)+A
#RANDBETWEEN(시작 수, 끝 수) ; 사이 정수 ※사이값에 정수 없으면 올림
#RANK.EQ(등수를 구하려는 셀, 범위, 차순) ; ※ 범위 $로 고정해주어야 함(-> RANK.AVG) ; 0:
내림
*=RANK( ) / 총 개수: 상위 % 구하기 -> 셀 서식-백분율 – 소수 자릿수 1
#RANK.AVG(등수를 구하려는 셀, 범위); 공동은 ?.5로 나옴
#REPLACE(텍스트,A,B,빠꿀 내용); 텍스트의 왼쪽 A번째 글자부터 B개를 ‘바꿀 내용’으로 바꾸
기
#REPT(“텍스트”, 반복횟수); 텍스트를 반복횟수만큼 반복한다. *REPT(B3&“ ”,3) 참조형식에 띄
어쓰기

*=REPT("★",H5)&REPT("☆",5-H5)

#Right(“텍스트”, N) 오른쪽에서 N개의 문자를 출력하는 함수

*=IF(RIGHT(B6,1)="M","남성용",IF(RIGHT(B6,1)="W","여성용",""))

#ROUND(수, N) ; 소수점 아래 N번째자리 까지 반올림
-2: 십의 자리, -1: 일의 자리, 0: 소수점 첫째 짜리, 1: 소수점 둘째 자리, 2: 소수점 셋째 자
리
#ROUNDDOWN(수, N) ; 소수점 아래 몇까지 내림
#ROUNDUP(수, N) ; 소수점 아래 몇까지 올림
#ROW(셀); 해당 셀의 행 번호를 구한다.
#SEARCH(“찾을 내용”, “전체 내용”, N) ; 전체 내용의 N번째 문자부터 시작하여 ‘찾을 내용’의
첫 문자
*=SEARCH(“(”, B2, 1) ; (가 몇 번째 내용인지 찾아라
#SMALL; N번째로 작은 값
#STDEV.S/P ; 4가지 종류 ; 표준편차 ; P: 모집단, A: 텍스트와 논리값 포함
#SUBTOTAL(함수 번호, 범위 1, 범위 2) ; 함수 번호 1: 평, 2: count~~
#SUBSTITUTE(텍스트,A, B ,N): N번째 A를 B로, SUBSTITUTE(텍스트, A, B);모든 A를 B로
#SUM( ) ; =sum(f3) ; 정의된 이름으로 수식 적용 ##합계
#SUMIF(조건에 맞는 셀을 찾을 범위, 조건, 합을 구할 범위) ^AVERAGEIF와 유사
※1:1 대응되어야 하므로, 조건에 맞는 셀을 찾을 범위의 모양과 크기가 갖아야 한다.
#DSUM( 데이터베이스, 필드, 조건 ): '데이터베이스' 범위에서 '조건' 범위의 설정을 만족하는
레코드를 추출하여, 그 중에서 합계를 구할 '필드' 의 합을 구하는 것이다.
#SUMPRODUCT(배열, 배열, 배열...); 배열 또는 범위의 대응되는 값끼리 곱해서 그 합을 구한다.
#TEXT(값, 셀 서식) ; TEXT(1,“0.0%”) ; 0.00: 소수점 2자리 *TEXT(B2, “0.00”)&“ / ”&TEXT(B3,
“0.00”)
※ 셀 서식은 사용자 서식 쓰듯이
#TODAY() ; 그냥 날짜로 입력하면 바뀌지 않는다. /사용해도 되고 –사용해도 된다.
#TRIM(“”): 공백 없애줌
#TRUNK(수, N) ; 버림
#QUOTIENT(A,B); A를 B로 나눈 몫(정수)
#VAR.S/P: 분산
#VLOOKUP(찾을 값, 표 범위, N, 논리값) ; V: Vertical ABC !~를 ~범위에서 ~칸 대충
표 범위는 $로 고정, N은 몇 번째 칸값 반환, 논리값 T는 대략|F는 정확히
#WEEKDAY(날짜, N) ; 기본이 1 ; N이 1: 일요일(1), N이 2: 월요일(1), N이 3: 월요일(0)
```

## 함수 활용
```java
IF, AND, OR, COUNTIF, COUNTA가 수식 안에 들어갈 때는 =을 뺀다

#글자 텍스트 !“” 표현, 숫자는 “” 없이 표현
#LEN(“텍스트”) ; 텍스트의 문자 수 구하는 함수
*Right(B3, D3-1)
#lLEFT *“네이버”&LEFT(“블로그씨”, 3)
*=IF(LEFT(B11,1)="L","좌안",IF(LEFT(B11,1)="R","우안",""))
#LEN(셀) ; 셀의 길이를 잰다
#LENB(셀) ; 셀의 길이를 바이트 단위로 잰다.(영어, 숫자 1바이트, 한글: 2바이트)
#MID(텍스트, A, B) ; 텍스트 내의 A번째 문자부터 B개의 문자를 출력하는 것이다.
#Right(“텍스트”, N) 오른쪽에서 N개의 문자를 출력하는 함수

*=IF(RIGHT(B6,1)="M","남성용",IF(RIGHT(B6,1)="W","여성용",""))
#REPT(“텍스트”, 반복횟수); 텍스트를 반복횟수만큼 반복한다. *REPT(B3&“ ”,3) 참조형식에 띄
어쓰기

*=REPT("★",H5)&REPT("☆",5-H5)
#REPLACE(텍스트,A,B,빠꿀 내용); 텍스트의 왼쪽 A번째 글자부터 B개를 ‘바꿀 내용’으로 바꾸
기
*REPLACE(C12, SEARCH(“ ”,C12),1,“”); 공백을 “”공백 없음으로 바꿔라
*REPLACE(C12,2,1,“”)
#SEARCH(“찾을 내용”, “전체 내용”, N) ; 전체 내용의 N번째 문자부터 시작하여 ‘찾을 내용’의
첫 문자
*=SEARCH(“(”, B2, 1) ; (가 몇 번째 내용인지 찾아라
#SUBSTITUTE(텍스트,A, B ,N): N번째 A를 B로, SUBSTITUTE(텍스트, A, B);모든 A를 B로
#TEXT(값, 셀 서식) ; TEXT(1,“0.0%”) ; 0.00: 소수점 2자리 *TEXT(B2, “0.00”)&“ / ”&TEXT(B3,
“0.00”)
```
